/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
import type { ArgumentProvider } from '../ArgumentProvider.js';
export declare const BROWSER_CONNECTION_ENV_VAR = "POWERAPPS_CLI_ENABLE_BROWSER_CONNECTION";
export declare const BROWSER_DISABLED_MESSAGE = "This connection requires user interaction. Please open https://make.powerapps.com to create a connection.";
export declare function isBrowserConnectionEnabled(): boolean;
/**
 * Create a connection for a connector.
 *
 * Behavior:
 *  - Fetches the connector definition.
 *  - If the connector has a single SSO-eligible auth type, attempts silent SSO
 *    via `createConnectionAsync` (no browser required).
 *  - Otherwise (or if SSO fails in interactive mode), opens the maaf-player
 *    connection-creation page in the browser and waits for the callback.
 *  - In non-interactive mode without SSO-eligibility, fails with a clear
 *    `UsageError` since there is no browser to fall back to.
 */
export declare function createConnectionVerbAsync(argumentProvider: ArgumentProvider): Promise<void>;
//# sourceMappingURL=CreateConnection.d.ts.map