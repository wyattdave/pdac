/*!
 * Copyright (C) Microsoft Corporation. All rights reserved.
 */
export interface TokenRequest {
    /** The resource identifier the token is requested for (e.g. 'https://service.powerapps.com'). */
    resource: string;
    /** Optional claims challenge (e.g. from a CAE flow). */
    claims?: string;
    /** Skip cached tokens and force a fresh token acquisition. */
    bypassCache?: boolean;
    /** Acquire token silently — fail rather than prompting the user interactively. */
    silent?: boolean;
}
/** Response containing an authentication token. */
export interface TokenResponse {
    /** The access token string. */
    accessToken: string;
    /** Token expiration time as an ISO 8601 string. Optional — when omitted, the cache falls back to the JWT `exp` claim. */
    expiresOn?: string;
}
/**
 * Authentication provider — REQUIRED, no default.
 * Used to acquire tokens when the SDK needs to call a service.
 */
export interface IAuthenticationProvider {
    getToken(request: TokenRequest): Promise<TokenResponse>;
}
//# sourceMappingURL=AuthenticationTypes.d.ts.map